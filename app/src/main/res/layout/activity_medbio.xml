<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white"
    android:padding="@dimen/_12sdp"
    tools:context=".MedbioActivity">
    <TextView
        android:id="@+id/authStatusTv2"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="Click button to scan fingerprint"
        android:textColor="@color/black"
        android:textSize="20sp" />
    <Button
        android:id="@+id/med_authBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:layout_below="@+id/authStatusTv2"
        android:layout_marginTop="30dp"
        android:backgroundTint="@color/black"
        android:drawableEnd="@drawable/ic_baseline_fingerprint_24"
        android:drawablePadding="10dp"
        android:text="@string/authenticate"
        android:textColor="@color/white"
        android:textSize="@dimen/_12ssp" />

</RelativeLayout>